Always read the repository before editing.

Never delete code unless instructed.

Create a plan before making changes.

Keep commits small.

After every change:

- npm install if required
- npm run build
- Explain errors
- Fix them automatically

Never modify frontend styling unless requested.